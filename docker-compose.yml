version: '3.1'

services:
  db:
    image: postgres
    restart: always
    container_name: simple-bank-db
    ports:
      - '2001:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password

